
<% @tasks.each do |task| %>
  <ul>
    <li><%= link_to task.title, task_path(task)%>
<%= link_to 'Delete', delete_path(task), method: :delete, data: {confirm: 'Are you sure you want to delete?'} %></li>
  </ul>
<% end %>

<%= link_to 'Add Task', new_path %>

